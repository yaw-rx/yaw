# @yaw-rx/vscode

VSCode extension. Injects TextMate grammars for HTML, CSS, TypeScript, and WGSL into `template` and `styles` string literals inside `@Component` decorators. Provides auto-completion and hover information for inline templates and styles.

```bash
npm install @yaw-rx/vscode --save-dev
npm run build
```
