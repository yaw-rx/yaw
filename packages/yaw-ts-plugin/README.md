# @yaw-rx/ts-plugin

TypeScript language service plugin. Provides type information for `@state` fields and their `$` observable getters in the IDE.

```bash
npm install @yaw-rx/ts-plugin --save-dev
npm run build
```
