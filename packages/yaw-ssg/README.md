# @yaw-rx/ssg

Static site generator. Uses Puppeteer to visit routes in parallel browser tabs, captures rendered HTML, and serializes component and service state into a hydration blob.

```bash
npm install @yaw-rx/ssg --save-dev
npm run build
```
