# @yaw-rx/core

Core runtime. Reactive state, component registration, dependency injection, template binding, directive system, routing, and SSG hydration.

```bash
npm install @yaw-rx/core
npm run build
```
